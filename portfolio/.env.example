# Backend API base URL. For local dev, point at your local backend:
#   NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
# For production, this is set as a Vercel project environment variable.
NEXT_PUBLIC_API_BASE_URL=https://portfolio-backend-68bh.onrender.com/api

# Used to build absolute URLs in sitemap.xml / robots.txt.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
